# Test and development dependencies, on top of requirements.txt.

-r requirements.txt

pytest~=9.1
pytest-cov~=7.1
httpx~=0.28

# The PostgreSQL driver lives in requirements.txt rather than here, because
# production runs on PostgreSQL and so does the suite - there is no second
# engine to fall back to, which is how a NUL byte in a text column was found
# before it reached a facility.

# pip-tools, which compiles the two lock files, is deliberately NOT listed
# here. It depends on pip and setuptools themselves, so a lock generated with it
# in the set pins pip inside a file that pip is being asked to install — which
# needs --allow-unsafe and installs a pip over the running one. Install it into
# a throwaway environment when the locks need regenerating; the command is in
# requirements.lock.

# Linting. `ruff format` is deliberately not enforced - see pyproject.toml.
ruff~=0.15

# Configured in pyproject.toml but not a CI gate yet: it reports 48 errors
# against the current code, almost all of them type-narrowing it cannot follow
# rather than defects. A gate that red is a gate somebody turns off. Run it
# locally, fix a file at a time, then make it a gate.
mypy~=1.18
