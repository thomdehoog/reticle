"""Test package for the Reticle backend.

Made a package so the modules can share helpers through ``from .conftest
import ...`` rather than through fixture indirection for things that are plain
functions.
"""
